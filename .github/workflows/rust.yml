# rust.yml — REMPLACÉ par ci.yml (T4.2)
# Ce fichier est conservé pour l'historique git mais désactivé.
# Voir .github/workflows/ci.yml pour le pipeline CI actif.
name: Rust CI (désactivé)

on:
  workflow_dispatch:   # déclenchement manuel uniquement

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow désactivé — voir ci.yml"
